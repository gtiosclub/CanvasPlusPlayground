//
//  Courses.swift
//  CanvasPlusPlayground
//
//  Created by Abdulaziz Albahar on 11/1/24.
//

import Foundation
/*
/// Allows us to cache network results that arrive wholly as arrays.
struct CacheableArray<Element: Cacheable>: IntCacheable {
    var tag: String = "courses"
    
    typealias CachedDTO = CanvasDTO<CacheableArray<Element>>
    var items: [Element]
    
    var id: Int?
    func toDTO() throws -> CachedDTO {
        try CanvasDTO(model: self)
    }
}
*/
